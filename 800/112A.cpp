#include <iostream>
#include <algorithm>

int main()
{
    std::string name;
    int count;

    std::cin >> name;
    std::sort(name.begin(), name.end());
    for(int i=0; i<name.size(); i++){
        i=
    }
    std::cout << name;
}